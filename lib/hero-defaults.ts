// Fallback si site_settings todavía no tiene fila (o falló la lectura) — mismos
// valores que el default de la columna en supabase/schema.sql.
export const DEFAULT_HERO_HEADLINE = "la akd quiere mantener el liderato";
export const DEFAULT_HERO_SUBTITLE =
  "Virtualmente con cupo en la final de Copa Pichincha y clasificados como primero de grupo, la AKD busca terminar el trabajo de buena manera en el último partido de esta fase.";
